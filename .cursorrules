# Dispatch-System 開發準則

## 1. 核心邏輯
- 目前版本：v7-map-first。
- AI 大腦路徑：`utils/ai_v7.js`。
- 核心目標：派單邏輯必須具備「地圖優先」驗證，並消除機械感。

## 2. 對話風格規則 (針對 Clawman 的需求)
- **簡潔第一**：回覆乘客字數嚴格控制在 30-50 字內。
- **在地口語**：使用台灣在地調度員語氣（例如：「收到，請問哪一區？」），禁止使用「親愛的顧客」等冗餘稱呼。
- **情緒權重**：在處理 `parseOrderFromText` 時，需判斷乘客是否有焦慮、酒醉等異常情緒，並在回覆中加入安撫語句。

## 3. 部署安全規範
- 每次修改 `server.js` 或 `utils/ai_v7.js` 後，必須自動更新 `package.json` 的版本號。
- 推送前確認：`git rev-parse --short HEAD` 與日誌中的 `railwayGitSha` 必須對齊。