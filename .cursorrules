## 0. 絕對分流與物理隔離 (HIGHEST PRIORITY)
- 系統必須嚴格區分「客群 (一般用戶)」與「司機主群 (ADMIN_GROUP)」。
- 客群邏輯：處理地圖與地址清洗（去除郵遞區號、國家、店家地標）。
- 司機邏輯：處理極簡黑話（如「林森10」、「汐止10」），代表報班與到達時間。
- 修改禁忌：修改客群功能時，絕對禁止覆蓋或干擾司機群邏輯。每次修改後必須自我檢查是否破壞分流。
- 程式碼產出要求：每個獨立的修改指令結束後，必須提醒開發者執行 `git push` 保存進度。
# Dispatch-System 開發準則與底層鐵律

## 1. 核心邏輯與絕對禁區 (CRITICAL)
- **版本路徑**：v7-map-first，AI 邏輯集中於 `utils/ai_v7.js`。
- **[鐵律] 地圖驗證不可繞過**：嚴禁 AI 擅自移除或簡化 Google Maps API 的實體驗證代碼。發送任何派車卡片前，必須確保 `pickup_verified` 是由地圖 API 回傳真實地址，**絕對禁止 AI 單方面腦補地址有效性**。
- **[鐵律] 系統時間鎖死**：嚴禁 AI 使用對話歷史中的舊日期。所有訂單的日期與時間，若無特別指定，必須強制透過 `new Date()` 抓取伺服器當下時間。
- **[鐵律] 禁亂加價**：`fare_surcharge` 預設必須為 0，除非明確出現特殊需求字眼，嚴禁 AI 自編理由加價。

## 2. 實戰派單流程規則 (一單一卡原則)
- **極速盲派**：上車地址經地圖驗證成功後，立即發送派車卡片，後續下車點於私訊補充。
- **未接單改單 (Unsend)**：若司機尚未接單，客人修改資訊時，系統必須呼叫 LINE API `unsendMessage` 收回舊卡片，再發送新卡片，保持版面乾淨。
- **已接單改單 (@標記)**：若訂單狀態為 `matched` (已接單)，嚴禁再次發送格式卡片。任何修改僅能以純文字 `@司機` 通知更新。

## 3. 對話風格規則 (派單員實戰語氣)
- **極度簡潔**：回覆字數嚴格控制在 30-50 字內，能短就短。
- **在地口語**：使用台灣調度員語氣（如：「收到，立刻派車」、「好，幫你更正」）。
- **杜絕廢話**：嚴禁「好的，幫您處理中」、「親愛的顧客」等機械式冗餘廢話。
- **情緒安撫**：若偵測到乘客焦慮或酒醉，才適度加入安撫語句。

## 4. 部署安全規範
- 每次修改 `server.js` 或 `utils/ai_v7.js` 後，必須手動/自動更新 `package.json` 的版本號。
- 推送前確認：`git rev-parse --short HEAD` 與日誌中的 `railwayGitSha` 必須對齊。